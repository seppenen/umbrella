
spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/auth/**
          filters:
            - StripPrefix=1
            - AddRequestHeader=X-Request, api-gateway
        - id: umbrella-service
          uri: lb://umbrella-service
          predicates:
            - Path=/umbrella/**
          filters:
            - StripPrefix=1
            - AddRequestHeader=X-Request, api-gateway

      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

  application:
    name: api-gateway
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_SERVER_HOST:localhost}:8761/eureka

server:
    port: ${API_GATEWAY_PORT:80}
logging:
    level:
      org.springframework.web: FALSE
